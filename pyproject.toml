[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "midwest-survey-models"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["scikit-learn>=1.8.0"]
